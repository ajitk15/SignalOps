# SignalOps v2 — agentic workflow platform.
# Only what currently has a consumer. LangGraph, langchain-anthropic, SQLAlchemy
# and the git/PR clients land with the phases that use them, so an unused
# dependency never sits here pretending to be load-bearing.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
httpx>=0.27.0
pyyaml>=6.0
python-dotenv>=1.0.0
